import 'package:flutter/material.dart';

// Define primary seed color (matching the image)
const Color primarySeedColor = Color(0xFF00BCD4); // Closer to the cyan/teal in the image

// Primary Colors (derived from the seed)
const Color primaryColor = primarySeedColor;
const Color primaryLightColor = Color(0xFFB2EBF2); // Lighter shade
const Color primaryDarkColor = Color(0xFF00838F); // Darker shade

// Secondary Colors (can be adjusted to complement primary, keeping current for now)
const Color secondaryColor = Color(0xFFFFC107); 
const Color secondaryLightColor = Color(0xFFFFECB3);
const Color secondaryDarkColor = Color(0xFFFFA000);

// Tertiary Colors (keeping current for now)
const Color tertiaryColor = Color(0xFF2196F3);
const Color tertiaryLightColor = Color(0xFFBBDEFB);
const Color tertiaryDarkColor = Color(0xFF1565C0);

// Error Colors (keeping current for now)
const Color errorColor = Color(0xFFF44336);
const Color errorLightColor = Color(0xFFEF9A9A);
const Color errorDarkColor = Color(0xFFD32F2F);

// Other Utility Colors (keeping current for now)
const Color warningColor = Color(0xFFFF9800);
const Color successColor = Color(0xFF4CAF50);
const Color infoColor = primaryColor; // Info color often matches primary

// Neutral Colors (for text, background, border, etc.)
const Color blackColor = Color(0xFF000000);
const Color whiteColor = Color(0xFFFFFFFF);
const Color greyColor = Color(0xFF9E9E9E);
const Color greyLightColor = Color(0xFFE0E0E0);
const Color greyDarkColor = Color(0xFF616161);
